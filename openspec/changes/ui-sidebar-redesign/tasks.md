## 1. 准备与遗漏评估（开发前）

- [x] 1.1 从最新 main 创建并切换开发分支 `feature/ui-sidebar-redesign`，验证 `git status` 干净且分支名正确
- [x] 1.2 完成设计稿 vs 现有功能遗漏评估表：逐项核对 9 个工具栏入口、3 个页签、行内编辑/后续记录/附件上传预览替换移除、变更记录与恢复、导出日志、云同步（自动备份/从云端同步/授权管理/测试连接/admin PIN）、登录与用户菜单、撤销恢复栈数据、localStorage key（`health_*_v1`、`health_chart_view`、`__userId` 后缀）在新设计中都有落点，评估结论追加到 design.md「遗漏评估」小节并确认无未决缺口
- [x] 1.3 将 `design-ref/pages/*.html` 与本次确认的三个决策（搜索+铃铛做真功能、维护页保留表格与日志、移动端抽屉）同步给实现者上下文，确认可直接开工

## 2. 外壳实现（AppShell）

- [x] 2.1 App.tsx 新增受控 `activeTab` state（默认 `list`）并接入 Radix Tabs `value`/`onValueChange`，验证三页签切换正常且默认选中数据列表
- [x] 2.2 新建 `src/app/components/shell/AppSidebar.tsx`：logo 区、五组导航（数据管理/报告/工具/同步/危险操作）、用户信息区；配色按 design.md 决策 3 映射（violet/blue 渐变 + 毛玻璃），验证侧边栏渲染且全部 11 个导航项可见
- [x] 2.3 侧边栏导航项接线：9 个动作项分别触发原有弹窗/动作（AddRecordDialog、IndicatorMaintenanceDialog、MedicalReportImportDialog、ConsultationBriefDialog、ImportRecordsDialog、ExportDialog、CloudSyncDialog、handleManualSync、ClearAllDataDialog），删除全部为危险色样式；验证每项点击后打开的原弹窗与重构前一致
- [x] 2.4 新建 `AppTopbar.tsx`：吸顶、左侧标题「数据概览」+副标题，右侧搜索框/铃铛/头像；`<lg` 显示汉堡按钮；验证滚动吸顶与按钮渲染
- [x] 2.5 用户菜单接入：顶栏头像与侧边栏底部用户区均打开现有 UserMenu 菜单（邮箱/设置登录密码/退出登录），未启用认证时不渲染用户区；验证登录态与未登录态两种渲染
- [x] 2.6 新建 `StatsCards.tsx`：三张卡片（总记录数/检验指标种类/最后更新）+ 趋势徽章（本月新增、覆盖类别数、距今时间），数值口径与重构前一致；验证数值与旧版一致
- [x] 2.7 移除旧顶部标题块与 9 按钮网格，整页跑一遍冒烟（添加一条记录、导出 Excel）验证无回归

## 3. 数据列表页

- [x] 3.1 按设计稿重排透视表卡片：卡头（标题「指标数据」+ 说明 + 分类下拉）+ 表格 + 卡底分页；分类切换逻辑复用现有 `indicatorDataCategoryId`；验证列头随分类变化
- [x] 3.2 实现客户端分页（每页 10 行）：页码按钮、上一页/下一页、当前页高亮、「共 N 条记录，显示第 X-Y 条」；验证翻页与记录数信息正确
- [x] 3.3 空分类与搜索无结果时显示空状态；验证空态文案出现且不报错

## 4. 数据维护页

- [x] 4.1 新建 `DataMaintenancePage.tsx`：2×2 启动卡片（检验指标维护/Excel 数据管理/报告导入管理/云端同步），四类按钮分别接线原弹窗与 `handleManualSync`；验证每个按钮行为与重构前一致
- [x] 4.2 卡片元信息接线：当前 N 个指标、支持 .xlsx, .csv、附件 N 份、同步状态徽章（已同步·最后同步时间/同步中/未配置）；验证元信息随数据实时变化
- [x] 4.3 危险区卡片：「删除全部数据」按钮复用现有确认对话框与 `handleClearAllRecords`；验证确认后清空、取消无副作用、变更日志出现 clear 记录
- [x] 4.4 启动卡下方保留分类筛选 + RecordTable + 变更记录列表并按设计稿卡片化重排（交互 handler 全部不变）：验证行内编辑、删除、新增后续、附件上传/预览/替换/移除、超标/偏低红徽章、恢复按钮全部可用

## 5. 图表分析页

- [x] 5.1 新建 `ChartAnalysisPage.tsx`：时间范围筛选组（7天/30天/90天/全部，默认全部）+ 分类下拉（沿用 `health_chart_view` 持久化）；验证筛选生效且刷新后恢复分类
- [x] 5.2 逐指标两列卡片网格：标题 + 变化徽章 + 折线/柱状/面积循环类型 + 最高/最低/平均/当前摘要，曲线用现有 8 色 palette、violet 轴色；验证多指标/单指标/空分类三种渲染
- [x] 5.3 综合雷达卡：≥3 个含最新值指标时显示（归一化 0-100、综合评分/最佳指标/待改善摘要），否则隐藏；验证两种分支
- [x] 5.4 移除旧 RecordChart 图表区挂载点（文件保留不删）；验证图表页无重复渲染且既有编辑能力可从数据维护页完成

## 6. 全局搜索与通知中心

- [x] 6.1 App 级 `searchQuery` state + 顶栏搜索框，数据列表/数据维护/图表分析三页接入过滤（日期/指标名/数值匹配，清空恢复）；验证三页过滤与恢复
- [x] 6.2 新建 `AppNotifications.tsx`：Popover 面板展示最近 10 条变更记录（四色徽章）+ 云同步状态行 + 「查看全部变更」跳转数据维护页签；验证有记录/空态/跳转三种情形

## 7. 移动端适配

- [x] 7.1 侧边栏 `<lg` 收起，汉堡按钮经 shadcn Sheet 抽屉渲染完整导航，选中项或点遮罩后关闭；验证 375px 视口下打开/选择/关闭
- [x] 7.2 统计卡、启动卡网格、表格区 `grid-cols-1 lg:grid-cols-*` 堆叠与横向滚动；验证 375px 无横向溢出、功能可用

## 8. 全面测试与验收

- [x] 8.1 自动化回归：`npx vitest run` 全绿（App.export.test.tsx 9 例 + MedicalReportImportDialog.test.tsx 9 例随 DOM 调整修正后仍全绿）、`tsc --noEmit` 无错误
- [x] 8.2 逐按钮桌面验收（1440px，对照 design-ref 截图）：侧边栏 11 项（含删除全部的确认流）、3 个页签、分类下拉×2、分页按钮组、时间范围 4 档、启动卡 6 个按钮（管理指标/导入Excel/导出Excel/导入报告/立即同步/删除全部数据）、搜索框、铃铛、头像菜单（设置登录密码/退出登录）、行内编辑/删除/后续/附件四组操作按钮、变更记录恢复按钮——每项记录通过/失败
- [x] 8.3 逐按钮移动端验收（375px，复用/扩展 `mobile_walkthrough.cjs`）：汉堡抽屉全项、表格横向滚动、附件预览、弹窗可用性
- [x] 8.4 配色走查：对照 design.md 决策 3 核对主色渐变、毛玻璃卡片、rose 危险、emerald 成功与重构前一致，截图留档（`take_screenshots.cjs`）
- [x] 8.5 数据安全走查：删除全部→恢复、云同步手动/自动触发、导出 Excel 与导入 Excel 往返一条真实记录，验证数据无损、localStorage key 未变
- [x] 8.6 汇总验收记录到本 change 目录（验收清单勾选 + 问题修复记录），确认全部通过后准备合并 PR
